import { HeroSection } from "@/components/hero-section";
import { AnimatedLeaves } from "@/components/animated-leaves";

const Index = () => {
  return (
    <div className="relative">
      <HeroSection />
      <AnimatedLeaves />
    </div>
  );
};

export default Index;
